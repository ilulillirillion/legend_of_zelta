#include "map.h"


void Map::generate() {
  
  for(int x = 0; x < _height; ++x) {
    for(int j = 0; j < _width; ++j {
      
      //double x = (double)x/((double) _width);
      //double y = (double)y/((double) _height);
      //double n = pn.noise(10 * x, 10 * y, 0.8);
      
      // Water
      
